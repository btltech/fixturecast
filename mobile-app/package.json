{
  "name": "fixturecast-mobile",
  "version": "1.0.0",
  "description": "FixtureCast Mobile App",
  "main": "index.js",
  "scripts": {
    "sync": "cap sync",
    "build": "cd .. && npm run build && cd mobile-app && cap sync",
    "android": "cap run android",
    "ios": "cap run ios",
    "open:android": "cap open android",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/push-notifications": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/browser": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
